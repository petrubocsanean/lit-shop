import { LitElement, html, css } from 'lit-element';

export class LitShop extends LitElement {
  static styles = css`
    :host {
      display: block;
    }
  `;

  render() {
    return html` Home `;
  }
}
