import { LitElement, html, css } from 'lit-element';

class CartView extends LitElement {
  static get styles() {
    return css``;
  }

  render() {
    return html`cart view template`;
  }
}

customElements.define('cart-view', CartView);
